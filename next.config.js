/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If your components use Next/Image and external images, uncomment and add domains:
  // images: { domains: ['images.unsplash.com'] },
}

module.exports = nextConfig
